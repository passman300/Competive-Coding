#include <bits/stdc++.h>
using namespace std;

typedef long long ll;

int N;

ll p;
ll a;

int main(){
    cin >> N;

    int x1, x2, y1, y2, z1, z2;

    cin >> x1 >> x2 >> y1 >> y2 >> z1 >> z2;

    ll a = (x2 - y2);
    ll b = (x1 - y1);

    ll c = (x2 - z2);
    ll d = (x1 - z1);

    ll e = (y2 - z2);
    ll f = (y1 - z1);


    

    p =  /  +  /  +  / ;

    // got to check what if the rise or run is zero
    // if so don't divide just include the other non-zero value

    if ()

    cout << p << endl;
}

